# 2026-08 Active Worklog

## 2026-08-30 — Checkpoint MVP-2

- Role: Codex knowledge checkpoint.
- Completed: accepted Phase 7 and MVP-2 with no open production finding, preserved raw state/worklog
  evidence and compacted the hot path into [MVP-2.md](../milestones/MVP-2.md).
- Validation: credential and synthetic-skip guards rejected invalid inputs; DB-free
  520/378/142/0/0; Oracle 520/520 with zero skip/failure/error; Frontend 97/97 plus production
  build; root diff check exit 0.
- Decisions: the optional LLM adapter and operational/external integrations remain deferred.
- Open: none for MVP-2.
- Next: wait for user-selected post-MVP or portfolio/release scope.
- Archive: [raw worklog through acceptance](../archive/worklogs/2026-08-30-through-phase7-acceptance.md).

## 2026-08-31 — Allocator workbench redesign implemented

- Role: Claude implementation, resumed from an uncommitted redesign lost to an out-of-session
  branch checkout (recovered via the intact spec + support files, not from memory).
- Completed: implemented [2026-08-30-allocator-workbench-redesign-spec.md](../state/2026-08-30-allocator-workbench-redesign-spec.md)
  in full -- `AllocatorWorkStatus` list filter/sort/summary API, open-transfer parity fix,
  comparison-only fail-closed on MANUAL and decision write, and the full Frontend redesign (summary
  tiles, work-status tabs, master-detail layout, 4-tab detail, auto-select/auto-simulate, approval
  confirm dialog, reason-code selects, terminology per spec section 5).
- Validation: seed-check passed; test-db-free 527/385/142/0/0; Oracle Backend 527/527 0/0/0;
  Frontend 106/106; production build passed; root diff check exit 0; browser acceptance against a
  real Oracle-backed run (갱신 → summary/tabs → 검토하기 → auto-select/auto-simulate → 이동 승인
  confirm → live list/summary refresh) confirmed end to end; 375px has no page-level horizontal
  scroll.
- Decisions: recreated the local Oracle volume from scratch (Flyway checksum drift on V7, unrelated
  to this session) after user confirmation, since destroying a Docker volume required explicit
  approval. Archived `V16` (extra demo-volume migration) unapplied under
  `knowledge/archive/unused-migrations/` -- it references product SKUs never seeded and is not part
  of the accepted redesign scope.
- Open: none blocking. `V16` and `data/seed/mvp2/*.csv`/`scripts/validate-seed.ps1` are noted as
  loose ends in `current-task.md`, not verified/fixed this session.
- Next: wait for user-selected scope.
